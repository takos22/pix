version: 2.1
setup: true
orbs:
  continuation: circleci/continuation@1.0.0
workflows:
  setup-workflows:
    jobs:
      - continuation/continue:
          name: Setup Pix Worflow
          configuration_path: .circleci/continue_config.yml
      - continuation/continue:
          name: Launch API Workflow
          configuration_path: api/.circleci/config.yml
